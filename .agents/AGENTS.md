# Project-Scoped Rules & Guidelines

- **Git Push Constraint:** Do not automatically execute `git push` or push code changes to the remote repository. Always wait for the user to explicitly say "push" or instruct you to push to GitHub.
